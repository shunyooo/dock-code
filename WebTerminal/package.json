{
  "name": "belve-web-terminal",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=../Sources/Belve/Resources/terminal-bundle.js --format=iife --minify"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
